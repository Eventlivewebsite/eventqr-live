import { prisma } from './src/lib/prisma.ts'; async function run() { try { const created = await prisma.user.create({ data: { email: 'admin@eventqr.live', name: 'Super Admin', password: 'admin123', role: 'SUPER_ADMIN' } }); console.log('\n? SUPER ADMIN ACCOUNT CREATED SUCCESSFULLY:'); console.log(JSON.stringify(created, null, 2)); } catch(e) { console.error('Creation error:', e.message); } process.exit(0); } run();
